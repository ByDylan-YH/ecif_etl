 drop view  if exists ${hiveconf:namespace}.prj_info;
 create view if not exists ${hiveconf:namespace}.prj_info (
 group_id comment '群组编号',
 src_cust_id comment '源系统客户号',
 cust_id comment '客户编号',
 bk_type_cd comment '业务类别代码',
 second_bk_type_cd comment '二级业务类别代码',
 item_id comment '项目ID',
 item_name comment '项目名称',
 item_abbr comment '项目简称',
 item_code comment '项目编码',
 item_type comment '项目类型',
 item_type_name comment '项目类型名称',
 app_user_id comment '申请人编号',
 app_user_name comment '申请人名称',
 sposn_user_org comment '发起人部门',
 sposn_user_org_name comment '发起人部门名称',
 sposn_user_belt_team comment '发起人所属团队',
 sposn_user_belt_team_name comment '发起人所属团队名称',
 item_belt_seg_id comment '项目所属阶段编号',
 item_belt_seg_name comment '项目所属阶段名称',
 item_proj_dt comment '项目立项日期',
 item_leader_id comment '项目负责人编号',
 item_leader_name comment '项目负责人名称',
 src_sys comment '来源系统',
 upd_sys comment '更新系统',
 data_crt_tm comment '数据创建时间',
 data_upd_tm comment '数据更新时间',
 etl_crt_tm comment 'ETL创建时间',
 etl_upd_tm comment 'ETL更新时间'
) COMMENT '项目总表'
 as
  select
   `group_id` as group_id,
   `src_cust_id` as src_cust_id,
   `cust_id` as cust_id,
   `bk_type_cd` as bk_type_cd,
   `second_bk_type_cd` as second_bk_type_cd,
   `item_id` as item_id,
   `item_name` as item_name,
   `item_abbr` as item_abbr,
   `item_code` as item_code,
   `item_type` as item_type,
   `item_type_name` as item_type_name,
   `app_user_id` as app_user_id,
   `app_user_name` as app_user_name,
   `sposn_user_org` as sposn_user_org,
   `sposn_user_org_name` as sposn_user_org_name,
   `sposn_user_belt_team` as sposn_user_belt_team,
   `sposn_user_belt_team_name` as sposn_user_belt_team_name,
   `item_belt_seg_id` as item_belt_seg_id,
   `item_belt_seg_name` as item_belt_seg_name,
   `item_proj_dt` as item_proj_dt,
   `item_leader_id` as item_leader_id,
   `item_leader_name` as item_leader_name,
   `src_sys` as src_sys,
   `upd_sys` as upd_sys,
   `data_crt_tm` as data_crt_tm,
   `data_upd_tm` as data_upd_tm,
   `etl_crt_tm` as etl_crt_tm,
   `etl_upd_tm` as etl_upd_tm
 from
  ${hiveconf:namespace}.ext_prj_bib_pmproj_full ;



 drop view  if exists ${hiveconf:namespace}.prj_th_fin_advr;
 create view if not exists ${hiveconf:namespace}.prj_th_fin_advr (
  group_id comment '群组编号',
  src_cust_id comment '源系统客户号',
  cust_id comment '客户编号',
  item_id comment '项目ID',
  stock_code comment '股票代码',
  fin_advr_fee comment '财务顾问费',
  has_attach1_flag comment '是否具备附件1标志',
  has_attach2_flag comment '是否具备附件2标志',
  risk_rak comment '风险等级',
  item_flow_id comment '项目流程编号',
  corp_governance comment '报告期内公司治理情况',
  conp_name comment '联系人名称',
  proj_app_dt comment '立项申请日期',
  sign_acct comment '签字会计师',
  sign_lawr comment '签字律师',
  sign_evaluatord comment '签字评估师',
  contractor_type comment '承揽方类型',
  contractor_info comment '承揽方信息',
  chag_clause comment '收费条款',
  intlacc_status comment '内核状态',
  has_been_filed_flag comment '是否已归档标志',
  filed_dt comment '归档日期',
  intlacc_app comment '内核申请',
  oth_regr_type comment '其他重组类型',
  other_megr_regr comment '其他并购重组',
  scma_introduction comment '方案介绍',
  subj_asset_profile comment '标的资产概况',
  trd_cntp_profile comment '交易对手概况',
  other_no_allow comment '其他非行政许可',
  other_item_fetur comment '其他项目特征',
  other_report_type comment '其他报告类型',
  item_belt_team comment '项目所属团队',
  quality_charge_person comment '质控对口人员',
  expe_formal_proj_dt comment '预计正式立项日期',
  expe_intlacc_dt comment '预计内核日期',
  expe_rep_dt comment '预计申报日期',
  expe_audit_dt comment '预计上会日期',
  expe_item_co_dt comment '预计项目实施日期',
  seal_flag comment '用印标志',
  promiser comment '承诺人',
  seal_itm_belt comment '用印事项归属',
  seal_itm_supplementary comment '用印事项说明',
  draw_supplementary comment '领取地点',
  seal_after_snd_flag comment '用印后寄送与否标志',
  seal_snd_address comment '用印寄送地址',
  seal_receivor comment '用印收件人',
  seal_receivor_tele comment '用印收件人电话',
  seal_consignee comment '领取人',
  seal_consignee_tele comment '领取人电话',
  approval_dt comment '批文日期',
  accpt_dt comment '受理日期',
  gh_dt comment '过会日期',
  stock_purs_dt comment '股票申购日期',
  stock_list_dt comment '股票上市日期',
  keep_supervision_maty_dt comment '持续督导期限日期',
  item_intlacc_dt comment '项目内核日期',
  item_rep_dt comment '项目申报日期',
  item_end_dt comment '项目终止日期',
  item_finish_dt comment '项目完结日期',
  src_sys comment '来源系统',
  upd_sys comment '更新系统',
  data_crt_tm comment '数据创建时间',
  data_upd_tm comment '数据更新时间',
  etl_crt_tm comment 'ETL创建时间',
  etl_upd_tm comment 'ETL更新时间'
  ) COMMENT '投行财务顾问项目表'
 as
  select
   `group_id` as group_id,
   `src_cust_id` as src_cust_id,
   `cust_id` as cust_id,
   `item_id` as item_id,
   `stock_code` as stock_code,
   `fin_advr_fee` as fin_advr_fee,
   `has_attach1_flag` as has_attach1_flag,
   `has_attach2_flag` as has_attach2_flag,
   `risk_rak` as risk_rak,
   `item_flow_id` as item_flow_id,
   `corp_governance` as corp_governance,
   `conp_name` as conp_name,
   `proj_app_dt` as proj_app_dt,
   `sign_acct` as sign_acct,
   `sign_lawr` as sign_lawr,
   `sign_evaluatord` as sign_evaluatord,
   `contractor_type` as contractor_type,
   `contractor_information` as contractor_information,
   `chag_clause` as chag_clause,
   `intlacc_status` as intlacc_status,
   `has_been_filed_flag` as has_been_filed_flag,
   `filed_dt` as filed_dt,
   `intlacc_app` as intlacc_app,
   `oth_regr_type` as oth_regr_type,
   `other_megr_regr` as other_megr_regr,
   `scma_introduction` as scma_introduction,
   `subj_asset_profile` as subj_asset_profile,
   `trd_cntp_profile` as trd_cntp_profile,
   `other_no_allow` as other_no_allow,
   `other_item_fetur` as other_item_fetur,
   `other_report_type` as other_report_type,
   `item_belt_team` as item_belt_team,
   `quality_charge_person` as quality_charge_person,
   `expe_formal_proj_dt` as expe_formal_proj_dt,
   `expe_intlacc_dt` as expe_intlacc_dt,
   `expe_rep_dt` as expe_rep_dt,
   `expe_audit_dt` as expe_audit_dt,
   `expe_item_co_dt` as expe_item_co_dt,
   `seal_flag` as seal_flag,
   `promiser` as promiser,
   `seal_itm_belt` as seal_itm_belt,
   `seal_itm_supplementary` as seal_itm_supplementary,
   `draw_supplementary` as draw_supplementary,
   `seal_after_snd_flag` as seal_after_snd_flag,
   `seal_snd_address` as seal_snd_address,
   `seal_receivor` as seal_receivor,
   `seal_receivor_tele` as seal_receivor_tele,
   `seal_consignee` as seal_consignee,
   `seal_consignee_tele` as seal_consignee_tele,
   `approval_dt` as approval_dt,
   `accpt_dt` as accpt_dt,
   `gh_dt` as gh_dt,
   `stock_purs_dt` as stock_purs_dt,
   `stock_list_dt` as stock_list_dt,
   `keep_supervision_maty_dt` as keep_supervision_maty_dt,
   `item_intlacc_dt` as item_intlacc_dt,
   `item_rep_dt` as item_rep_dt,
   `item_end_dt` as item_end_dt,
   `item_finish_dt` as item_finish_dt,
   `src_sys` as src_sys,
   `upd_sys` as upd_sys,
   `data_crt_tm` as data_crt_tm,
   `data_upd_tm` as data_upd_tm,
   `etl_crt_tm` as etl_crt_tm,
   `etl_upd_tm` as etl_upd_tm
 from
  ${hiveconf:namespace}.ext_prj_bib_pmproj_full where trim(second_bk_type_cd)='1101';




 drop view  if exists ${hiveconf:namespace}.prj_th_stor_fin;
 create view if not exists ${hiveconf:namespace}.prj_th_stor_fin (
  group_id comment '群组编号',
  src_cust_id comment '源系统客户号',
  cust_id comment '客户编号',
  item_id comment '项目ID',
  stock_code comment '股票代码',
  conp_id comment '联系人编号',
  fin_advr_fee comment '财务顾问费',
  has_attach1_flag comment '是否具备附件1标志',
  has_attach2_flag comment '是否具备附件2标志',
  risk_rak comment '风险等级',
  item_flow_id comment '项目流程编号',
  corp_governance comment '报告期内公司治理情况',
  proj_app_dt comment '立项申请日期',
  sign_acct comment '签字会计师',
  sign_lawr comment '签字律师',
  sign_evaluatord comment '签字评估师',
  contractor_type comment '承揽方类型',
  contractor_information comment '承揽方信息',
  chag_clause comment '收费条款',
  intlacc_status comment '内核状态',
  has_been_filed_flag comment '是否已归档标志',
  filed_dt comment '归档日期',
  intlacc_app comment '内核申请',
  item_belt_team comment '项目所属团队',
  quality_charge_person comment '质控对口人员',
  expe_formal_proj_dt comment '预计正式立项日期',
  expe_intlacc_dt comment '预计内核日期',
  expe_rep_dt comment '预计申报日期',
  seal_flag comment '用印标志',
  promiser comment '承诺人',
  seal_itm_belt comment '用印事项归属',
  seal_itm_spl comment '用印事项说明',
  draw_plc comment '领取地点',
  seal_after_snd_flag comment '用印后寄送与否标志',
  seal_snd_address comment '用印寄送地址',
  seal_receivor comment '用印收件人',
  seal_receivor_tele comment '用印收件人电话',
  seal_consignee comment '领取人',
  seal_consignee_tele comment '领取人电话',
  approval_dt comment '批文日期',
  accpt_dt comment '受理日期',
  gh_dt comment '过会日期',
  stock_purs_dt comment '股票申购日期',
  stock_list_dt comment '股票上市日期',
  keep_supervision_maty_dt comment '持续督导期限日期',
  item_intlacc_dt comment '项目内核日期',
  item_rep_dt comment '项目申报日期',
  item_end_dt comment '项目终止日期',
  listedis comment '上市地',
  boar comment '板块',
  is_joint_spon_flag comment '是否联席保荐标志',
  is_chief_spon_flag comment '是否第一保荐人标志',
  is_joint_underwriter_flag comment '是否联席主承标志',
  expe_fb_dt comment '预计反馈日期',
  expe_premeeting_dt comment '预计初审会日期',
  expe_trialmeeting_dt comment '预计发审会日期',
  expe_item_iss_dt comment '预计项目发行日期',
  rcfs_dt comment '招股日期',
  stor_reg_dt comment '股权登记日期',
  src_sys comment '来源系统',
  upd_sys comment '更新系统',
  data_crt_tm comment '数据创建时间',
  data_upd_tm comment '数据更新时间',
  etl_crt_tm comment 'ETL创建时间',
  etl_upd_tm comment 'ETL更新时间'
  ) COMMENT '投行股权融资项目表'
 as
  select
   `group_id` as group_id,
   `src_cust_id` as src_cust_id,
   `cust_id` as cust_id,
   `item_id` as item_id,
   `stock_code` as stock_code,
   `conp_id` as conp_id,
   `fin_advr_fee` as fin_advr_fee,
   `has_attach1_flag` as has_attach1_flag,
   `has_attach2_flag` as has_attach2_flag,
   `risk_rak` as risk_rak,
   `item_flow_id` as item_flow_id,
   `corp_governance` as corp_governance,
   `proj_app_dt` as proj_app_dt,
   `sign_acct` as sign_acct,
   `sign_lawr` as sign_lawr,
   `sign_evaluatord` as sign_evaluatord,
   `contractor_type` as contractor_type,
   `contractor_information` as contractor_information,
   `chag_clause` as chag_clause,
   `intlacc_status` as intlacc_status,
   `has_been_filed_flag` as has_been_filed_flag,
   `filed_dt` as filed_dt,
   `intlacc_app` as intlacc_app,
   `item_belt_team` as item_belt_team,
   `quality_charge_person` as quality_charge_person,
   `expe_formal_proj_dt` as expe_formal_proj_dt,
   `expe_intlacc_dt` as expe_intlacc_dt,
   `expe_rep_dt` as expe_rep_dt,
   `seal_flag` as seal_flag,
   `promiser` as promiser,
   `seal_itm_belt` as seal_itm_belt,
   `seal_itm_supplementary` as seal_itm_supplementary,
   `draw_supplementary` as draw_supplementary,
   `seal_after_snd_flag` as seal_after_snd_flag,
   `seal_snd_address` as seal_snd_address,
   `seal_receivor` as seal_receivor,
   `seal_receivor_tele` as seal_receivor_tele,
   `seal_consignee` as seal_consignee,
   `seal_consignee_tele` as seal_consignee_tele,
   `approval_dt` as approval_dt,
   `accpt_dt` as accpt_dt,
   `gh_dt` as gh_dt,
   `stock_purs_dt` as stock_purs_dt,
   `stock_list_dt` as stock_list_dt,
   `keep_supervision_maty_dt` as keep_supervision_maty_dt,
   `item_intlacc_dt` as item_intlacc_dt,
   `item_rep_dt` as item_rep_dt,
   `item_end_dt` as item_end_dt,
   `listedis` as listedis,
   `boar` as boar,
   `is_joint_spon_flag` as is_joint_spon_flag,
   `is_chief_spon_flag` as is_chief_spon_flag,
   `is_joint_underwriter_flag` as is_joint_underwriter_flag,
   `expe_fb_dt` as expe_fb_dt,
   `expe_premeeting_dt` as expe_premeeting_dt,
   `expe_trialmeeting_dt` as expe_trialmeeting_dt,
   `expe_item_iss_dt` as expe_item_iss_dt,
   `rcfs_dt` as rcfs_dt,
   `stor_reg_dt` as stor_reg_dt,
   `src_sys` as src_sys,
   `upd_sys` as upd_sys,
   `data_crt_tm` as data_crt_tm,
   `data_upd_tm` as data_upd_tm,
   `etl_crt_tm` as etl_crt_tm,
   `etl_upd_tm` as etl_upd_tm
 from
  ${hiveconf:namespace}.ext_prj_bib_pmproj_full where trim(second_bk_type_cd)='1102';




 drop view  if exists ${hiveconf:namespace}.prj_th_neeq_derive;
 create view if not exists ${hiveconf:namespace}.prj_th_neeq_derive (
  group_id comment '群组编号',
  src_cust_id comment '源系统客户号',
  cust_id comment '客户编号',
  item_id comment '项目ID',
  item_flow_id comment '项目流程编号',
  conp_name comment '联系人名称',
  proj_app_dt comment '立项申请日期',
  contractor_type comment '承揽方类型',
  contractor_information comment '承揽方信息',
  intlacc_status comment '内核状态',
  has_been_filed_flag comment '是否已归档标志',
  filed_dt comment '归档日期',
  intlacc_app comment '内核申请',
  item_belt_team comment '项目所属团队',
  expe_rep_dt comment '预计申报日期',
  iss_scal comment '发行规模',
  rep_stuff_has_agreement_flag comment '申报材料是否包含协议标志',
  rep_stuff_has_promise_flag comment '申报材料是否包含承诺事项标志',
  last2yreve_cpgrowrate comment '近2年收入复合增长率',
  need_frontend_check_flag comment '是否需要前端校验标志',
  sign_agmt_flag comment '签署协议标志',
  subscr_pr comment '签约进度',
  src_sys comment '来源系统',
  upd_sys comment '更新系统',
  data_crt_tm comment '数据创建时间',
  data_upd_tm comment '数据更新时间',
  etl_crt_tm comment 'ETL创建时间',
  etl_upd_tm comment 'ETL更新时间'
  ) COMMENT '投行新三板衍生项目表'
 as
  select
   `group_id` as group_id,
   `src_cust_id` as src_cust_id,
   `cust_id` as cust_id,
   `item_id` as item_id,
   `item_flow_id` as item_flow_id,
   `conp_name` as conp_name,
   `proj_app_dt` as proj_app_dt,
   `contractor_type` as contractor_type,
   `contractor_information` as contractor_information,
   `intlacc_status` as intlacc_status,
   `has_been_filed_flag` as has_been_filed_flag,
   `filed_dt` as filed_dt,
   `intlacc_app` as intlacc_app,
   `item_belt_team` as item_belt_team,
   `expe_rep_dt` as expe_rep_dt,
   `iss_scal` as iss_scal,
   `rep_stuff_has_agreement_flag` as rep_stuff_has_agreement_flag,
   `rep_stuff_has_promise_flag` as rep_stuff_has_promise_flag,
   `last2yreve_cpgrowrate` as last2yreve_cpgrowrate,
   `need_frontend_check_flag` as need_frontend_check_flag,
   `sign_agmt_flag` as sign_agmt_flag,
   `subscr_pr` as subscr_pr,
   `src_sys` as src_sys,
   `upd_sys` as upd_sys,
   `data_crt_tm` as data_crt_tm,
   `data_upd_tm` as data_upd_tm,
   `etl_crt_tm` as etl_crt_tm,
   `etl_upd_tm` as etl_upd_tm
 from
  ${hiveconf:namespace}.ext_prj_bib_pmproj_full where trim(second_bk_type_cd)='1103';




 drop view  if exists ${hiveconf:namespace}.prj_th_neeq_recmd;
 create view if not exists ${hiveconf:namespace}.prj_th_neeq_recmd (
  group_id comment '群组编号',
  src_cust_id comment '源系统客户号',
  cust_id comment '客户编号',
  item_id comment '项目ID',
  item_flow_id comment '项目流程编号',
  conp_name comment '联系人名称',
  proj_app_dt comment '立项申请日期',
  contractor_type comment '承揽方类型',
  contractor_information comment '承揽方信息',
  intlacc_status comment '内核状态',
  has_been_filed_flag comment '是否已归档标志',
  filed_dt comment '归档日期',
  intlacc_app comment '内核申请',
  item_belt_team comment '项目所属团队',
  expe_rep_dt comment '预计申报日期',
  iss_scal comment '发行规模',
  rep_stuff_has_agreement_flag comment '申报材料是否包含协议标志',
  rep_stuff_has_promise_flag comment '申报材料是否包含承诺事项标志',
  last2yreve_cpgrowrate comment '近2年收入复合增长率',
  need_frontend_check_flag comment '是否需要前端校验标志',
  sign_agmt_flag comment '签署协议标志',
  subscr_pr comment '签约进度',
  keep_supervision_fee comment '持续督导费',
  ctr_amt comment '合同金额',
  expe_capt_amount comment '预计股本数',
  keep_supervision comment '持续督导人',
  src_sys comment '来源系统',
  upd_sys comment '更新系统',
  data_crt_tm comment '数据创建时间',
  data_upd_tm comment '数据更新时间',
  etl_crt_tm comment 'ETL创建时间',
  etl_upd_tm comment 'ETL更新时间'
  ) COMMENT '投行新三板推荐项目表'
 as
  select
   `group_id` as group_id,
   `src_cust_id` as src_cust_id,
   `cust_id` as cust_id,
   `item_id` as item_id,
   `item_flow_id` as item_flow_id,
   `conp_name` as conp_name,
   `proj_app_dt` as proj_app_dt,
   `contractor_type` as contractor_type,
   `contractor_information` as contractor_information,
   `intlacc_status` as intlacc_status,
   `has_been_filed_flag` as has_been_filed_flag,
   `filed_dt` as filed_dt,
   `intlacc_app` as intlacc_app,
   `item_belt_team` as item_belt_team,
   `expe_rep_dt` as expe_rep_dt,
   `iss_scal` as iss_scal,
   `rep_stuff_has_agreement_flag` as rep_stuff_has_agreement_flag,
   `rep_stuff_has_promise_flag` as rep_stuff_has_promise_flag,
   `last2yreve_cpgrowrate` as last2yreve_cpgrowrate,
   `need_frontend_check_flag` as need_frontend_check_flag,
   `sign_agmt_flag` as sign_agmt_flag,
   `subscr_pr` as subscr_pr,
   `keep_supervision_fee` as keep_supervision_fee,
   `ctr_amt` as ctr_amt,
   `expe_capt_amount` as expe_capt_amount,
   `keep_supervision` as keep_supervision,
   `src_sys` as src_sys,
   `upd_sys` as upd_sys,
   `data_crt_tm` as data_crt_tm,
   `data_upd_tm` as data_upd_tm,
   `etl_crt_tm` as etl_crt_tm,
   `etl_upd_tm` as etl_upd_tm
 from
  ${hiveconf:namespace}.ext_prj_bib_pmproj_full where trim(second_bk_type_cd)='1104';




 drop view  if exists ${hiveconf:namespace}.prj_gs_bond_iss;
 create view if not exists ${hiveconf:namespace}.prj_gs_bond_iss (
  group_id comment '群组编号',
  src_cust_id comment '源系统客户号',
  cust_id comment '客户编号',
  item_id comment '项目ID',
  item_flow_id comment '项目流程编号',
  conp_name comment '联系人名称',
  proj_app_dt comment '立项申请日期',
  intlacc_status comment '内核状态',
  has_been_filed_flag comment '是否已归档标志',
  filed_dt comment '归档日期',
  intlacc_app comment '内核申请',
  item_belt_team comment '项目所属团队',
  expe_rep_dt comment '预计申报日期',
  spca_maty_clause comment '特殊期限条款',
  incretrust_measure comment '增信措施',
  pty_rat comment '主体评级',
  iss_mode comment '发行方式',
  iss_mkt comment '发行市场',
  bondcode comment '债券代码',
  bond_rat comment '债项评级',
  coll_cptl_uses comment '募集资金用途',
  is_joint_leadunderwriter_flag comment '是否联席主承销商标志',
  is_leadunderwriter_flag comment '是否牵头主承销商标志',
  thip_leadunderwriter comment '第三方主承销商',
  in_excesscap_industry_flag comment '产能过剩行业标志',
  iss_scal_netassets_ratio comment '发行规模占净资产比例',
  assetliab_ratio_after comment '发债后资产负债率',
  guarantassets_ratio comment '对外担保占净资产比例',
  rat_perspective comment '评级展望',
  is_proj_prostandard_flag comment '是否符合立项标准标志',
  is_strict_audit_flag comment '是否属从严格审核标志',
  pty_rat_org comment '主体评级机构',
  pty_rat_dt comment '主体评级日期',
  debt_rat_org comment '债项评级机构',
  debt_rat_dt comment '债项评级日期',
  gutrer comment '担保人',
  gutrer_et comment '担保人企业性质',
  gutrer_rat comment '担保人评级',
  gutrer_rat_corp comment '担保人评级公司',
  gutrer_rat_dt comment '担保人评级日期',
  gutrer_guarbalance comment '担保人担保余额',
  gutrer_gbala_ratio comment '担保人担保余额占净资产比例',
  pawn_evaluation_value comment '抵/质押物评估值',
  pawn_evaval_ratio comment '抵/质押物评估值与净资产比例',
  pawn_eval_institution comment '抵/质押物评估机构',
  vested_industry comment '行业归属',
  maty_stru comment '期限结构',
  operts_id comment '操作人编号',
  opre_dt comment '操作日期',
  covenants_flag comment '承诺事项标志',
  exch_fb_flag comment '交易所反馈标志',
  stuff_rep_status comment '材料申报状态',
  rat_flag comment '评级标志',
  approval_no comment '批文编号',
  approval_check_dt comment '批文核准日期',
  approval_validity_end_dt comment '批文有效结束日期',
  first_iss_end_dt comment '首期发行结束日期',
  is_trusteecreditor_agent comment '受托管理人/债权代理人标志',
  amor_iss_flag comment '分期发行标志',
  amor_num comment '分期数',
  thim_bond_iss_scal comment '本次债券发行规模',
  approval_period_structure comment '批文期限结构',
  approval_iss_scal comment '批文发行规模',
  src_sys comment '来源系统',
  upd_sys comment '更新系统',
  data_crt_tm comment '数据创建时间',
  data_upd_tm comment '数据更新时间',
  etl_crt_tm comment 'ETL创建时间',
  etl_upd_tm comment 'ETL更新时间'
  ) COMMENT '固收债券发行项目表'
 as
  select
   `group_id` as group_id,
   `src_cust_id` as src_cust_id,
   `cust_id` as cust_id,
   `item_id` as item_id,
   `item_flow_id` as item_flow_id,
   `conp_name` as conp_name,
   `proj_app_dt` as proj_app_dt,
   `intlacc_status` as intlacc_status,
   `has_been_filed_flag` as has_been_filed_flag,
   `filed_dt` as filed_dt,
   `intlacc_app` as intlacc_app,
   `item_belt_team` as item_belt_team,
   `expe_rep_dt` as expe_rep_dt,
   `spca_maty_clause` as spca_maty_clause,
   `incretrust_measure` as incretrust_measure,
   `pty_rat` as pty_rat,
   `iss_mode` as iss_mode,
   `iss_mkt` as iss_mkt,
   `bondcode` as bondcode,
   `bond_rat` as bond_rat,
   `coll_cptl_uses` as coll_cptl_uses,
   `is_joint_leadunderwriter_flag` as is_joint_leadunderwriter_flag,
   `is_leadunderwriter_flag` as is_leadunderwriter_flag,
   `thip_leadunderwriter` as thip_leadunderwriter,
   `in_excesscap_industry_flag` as in_excesscap_industry_flag,
   `iss_scal_netassets_ratio` as iss_scal_netassets_ratio,
   `assetliab_ratio_after` as assetliab_ratio_after,
   `guarantassets_ratio` as guarantassets_ratio,
   `rat_perspective` as rat_perspective,
   `is_proj_prostandard_flag` as is_proj_prostandard_flag,
   `is_strict_audit_flag` as is_strict_audit_flag,
   `pty_rat_org` as pty_rat_org,
   `pty_rat_dt` as pty_rat_dt,
   `debt_rat_org` as debt_rat_org,
   `debt_rat_dt` as debt_rat_dt,
   `gutrer` as gutrer,
   `gutrer_et` as gutrer_et,
   `gutrer_rat` as gutrer_rat,
   `gutrer_rat_corp` as gutrer_rat_corp,
   `gutrer_rat_dt` as gutrer_rat_dt,
   `gutrer_guarbalance` as gutrer_guarbalance,
   `gutrer_gbala_ratio` as gutrer_gbala_ratio,
   `pawn_evaluation_value` as pawn_evaluation_value,
   `pawn_evaval_ratio` as pawn_evaval_ratio,
   `pawn_eval_institution` as pawn_eval_institution,
   `vested_industry` as vested_industry,
   `maty_stru` as maty_stru,
   `operts_id` as operts_id,
   `opre_dt` as opre_dt,
   `covenants_flag` as covenants_flag,
   `exch_fb_flag` as exch_fb_flag,
   `stuff_rep_status` as stuff_rep_status,
   `rat_flag` as rat_flag,
   `approval_no` as approval_no,
   `approval_check_dt` as approval_check_dt,
   `approval_validity_end_dt` as approval_validity_end_dt,
   `first_iss_end_dt` as first_iss_end_dt,
   `is_trusteecreditor_agent` as is_trusteecreditor_agent,
   `amor_iss_flag` as amor_iss_flag,
   `amor_num` as amor_num,
   `thim_bond_iss_scal` as thim_bond_iss_scal,
   `approval_period_structure` as approval_period_structure,
   `approval_iss_scal` as approval_iss_scal,
   `src_sys` as src_sys,
   `upd_sys` as upd_sys,
   `data_crt_tm` as data_crt_tm,
   `data_upd_tm` as data_upd_tm,
   `etl_crt_tm` as etl_crt_tm,
   `etl_upd_tm` as etl_upd_tm
 from
  ${hiveconf:namespace}.ext_prj_bib_pmproj_full where trim(second_bk_type_cd)='1201';




 drop view  if exists ${hiveconf:namespace}.prj_cw_neeq_derive;
 create view if not exists ${hiveconf:namespace}.prj_cw_neeq_derive (
  group_id comment '群组编号',
  src_cust_id comment '源系统客户号',
  cust_id comment '客户编号',
  item_id comment '项目ID',
  item_flow_id comment '项目流程编号',
  conp_name comment '联系人名称',
  proj_app_dt comment '立项申请日期',
  contractor_type comment '承揽方类型',
  contractor_information comment '承揽方信息',
  intlacc_status comment '内核状态',
  has_been_filed_flag comment '是否已归档标志',
  filed_dt comment '归档日期',
  intlacc_app comment '内核申请',
  item_belt_team comment '项目所属团队',
  expe_rep_dt comment '预计申报日期',
  iss_scal comment '发行规模',
  rep_stuff_has_agreement_flag comment '申报材料是否包含协议标志',
  rep_stuff_has_promise_flag comment '申报材料是否包含承诺事项标志',
  last2yreve_cpgrowrate comment '近2年收入复合增长率',
  need_frontend_check_flag comment '是否需要前端校验标志',
  sign_agmt_flag comment '签署协议标志',
  subscr_pr comment '签约进度',
  src_sys comment '来源系统',
  upd_sys comment '更新系统',
  data_crt_tm comment '数据创建时间',
  data_upd_tm comment '数据更新时间',
  etl_crt_tm comment 'ETL创建时间',
  etl_upd_tm comment 'ETL更新时间'
  ) COMMENT '场外新三板衍生项目表'
 as
  select
   `group_id` as group_id,
   `src_cust_id` as src_cust_id,
   `cust_id` as cust_id,
   `item_id` as item_id,
   `item_flow_id` as item_flow_id,
   `conp_name` as conp_name,
   `proj_app_dt` as proj_app_dt,
   `contractor_type` as contractor_type,
   `contractor_information` as contractor_information,
   `intlacc_status` as intlacc_status,
   `has_been_filed_flag` as has_been_filed_flag,
   `filed_dt` as filed_dt,
   `intlacc_app` as intlacc_app,
   `item_belt_team` as item_belt_team,
   `expe_rep_dt` as expe_rep_dt,
   `iss_scal` as iss_scal,
   `rep_stuff_has_agreement_flag` as rep_stuff_has_agreement_flag,
   `rep_stuff_has_promise_flag` as rep_stuff_has_promise_flag,
   `last2yreve_cpgrowrate` as last2yreve_cpgrowrate,
   `need_frontend_check_flag` as need_frontend_check_flag,
   `sign_agmt_flag` as sign_agmt_flag,
   `subscr_pr` as subscr_pr,
   `src_sys` as src_sys,
   `upd_sys` as upd_sys,
   `data_crt_tm` as data_crt_tm,
   `data_upd_tm` as data_upd_tm,
   `etl_crt_tm` as etl_crt_tm,
   `etl_upd_tm` as etl_upd_tm
 from
  ${hiveconf:namespace}.ext_prj_bib_pmproj_full where trim(second_bk_type_cd)='1301';




 drop view  if exists ${hiveconf:namespace}.prj_cw_neeq_recmd;
 create view if not exists ${hiveconf:namespace}.prj_cw_neeq_recmd (
  group_id comment '群组编号',
  src_cust_id comment '源系统客户号',
  cust_id comment '客户编号',
  item_id comment '项目ID',
  item_flow_id comment '项目流程编号',
  conp_name comment '联系人名称',
  proj_app_dt comment '立项申请日期',
  contractor_type comment '承揽方类型',
  contractor_information comment '承揽方信息',
  intlacc_status comment '内核状态',
  has_been_filed_flag comment '是否已归档标志',
  filed_dt comment '归档日期',
  intlacc_app comment '内核申请',
  item_belt_team comment '项目所属团队',
  expe_rep_dt comment '预计申报日期',
  iss_scal comment '发行规模',
  rep_stuff_has_agreement_flag comment '申报材料是否包含协议标志',
  rep_stuff_has_promise_flag comment '申报材料是否包含承诺事项标志',
  last2yreve_cpgrowrate comment '近2年收入复合增长率',
  need_frontend_check_flag comment '是否需要前端校验标志',
  sign_agmt_flag comment '签署协议标志',
  subscr_pr comment '签约进度',
  keep_supervision_fee comment '持续督导费',
  ctr_amt comment '合同金额',
  expe_capt_amount comment '预计股本数',
  keep_supervision comment '持续督导人',
  src_sys comment '来源系统',
  upd_sys comment '更新系统',
  data_crt_tm comment '数据创建时间',
  data_upd_tm comment '数据更新时间',
  etl_crt_tm comment 'ETL创建时间',
  etl_upd_tm comment 'ETL更新时间'
  ) COMMENT '场外新三板推荐项目表'
 as
  select
   `group_id` as group_id,
   `src_cust_id` as src_cust_id,
   `cust_id` as cust_id,
   `item_id` as item_id,
   `item_flow_id` as item_flow_id,
   `conp_name` as conp_name,
   `proj_app_dt` as proj_app_dt,
   `contractor_type` as contractor_type,
   `contractor_information` as contractor_information,
   `intlacc_status` as intlacc_status,
   `has_been_filed_flag` as has_been_filed_flag,
   `filed_dt` as filed_dt,
   `intlacc_app` as intlacc_app,
   `item_belt_team` as item_belt_team,
   `expe_rep_dt` as expe_rep_dt,
   `iss_scal` as iss_scal,
   `rep_stuff_has_agreement_flag` as rep_stuff_has_agreement_flag,
   `rep_stuff_has_promise_flag` as rep_stuff_has_promise_flag,
   `last2yreve_cpgrowrate` as last2yreve_cpgrowrate,
   `need_frontend_check_flag` as need_frontend_check_flag,
   `sign_agmt_flag` as sign_agmt_flag,
   `subscr_pr` as subscr_pr,
   `keep_supervision_fee` as keep_supervision_fee,
   `ctr_amt` as ctr_amt,
   `expe_capt_amount` as expe_capt_amount,
   `keep_supervision` as keep_supervision,
   `src_sys` as src_sys,
   `upd_sys` as upd_sys,
   `data_crt_tm` as data_crt_tm,
   `data_upd_tm` as data_upd_tm,
   `etl_crt_tm` as etl_crt_tm,
   `etl_upd_tm` as etl_upd_tm
 from 
  ${hiveconf:namespace}.ext_prj_bib_pmproj_full where trim(second_bk_type_cd)='1302';
